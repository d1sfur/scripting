using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Helloworld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Вывод текста в начале игры
        print("Papsdpa");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
